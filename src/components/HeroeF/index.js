import React from 'react'
import './index.css'

const HeroeF = ({name, origin}) => {

 

    return (
        <>
          <h4>{name}</h4>
          <small>{origin}</small>
        </>
    )
}
export default HeroeF;